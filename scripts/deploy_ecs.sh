#!/usr/bin/env bash
set -euo pipefail

# deploy_ecs.sh
# Reference helper for pushing images to ECR.
# The primary deployment method for this assignment is Terraform:
#   cd terraform/part3-ecs && terraform apply -auto-approve
#   cd terraform/part3-ecs && ./scripts/build-and-push.sh
# This script is kept for reference only.

echo "Use the Terraform config under terraform/part3-ecs instead."
exit 0